--- 
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: pvc-nfs-dynamic
spec:
  storageClassName: nfs-csi
  volumeName: pvc-mosquitto
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  