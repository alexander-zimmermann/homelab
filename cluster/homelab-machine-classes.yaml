###############################################################################
## Production machine class
###############################################################################
metadata:
  id: control-plane-prod
  type: MachineClasses.omni.sidero.dev
  namespace: default
spec:
  matchlabels:
    - "cluster = homelab, environment = prod, role = control-plane"
---
metadata:
  id: data-plane-prod
  type: MachineClasses.omni.sidero.dev
  namespace: default
spec:
  matchlabels:
    - "cluster = homelab, environment = prod, role = data-plane"

---
###############################################################################
## Development machine class
###############################################################################
metadata:
  id: control-plane-dev
  type: MachineClasses.omni.sidero.dev
  namespace: default
spec:
  matchlabels:
    - "cluster = homelab, environment = dev, role = control-plane"
---
metadata:
  id: data-plane-dev
  type: MachineClasses.omni.sidero.dev
  namespace: default
spec:
  matchlabels:
    - "cluster = homelab, environment = dev, role = data-plane"
