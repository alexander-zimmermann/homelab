pve_cluster:
  api_connection:
    api_url: "https://pve-1.zimmermann.eu.com:8006/api2/json"
    username: "root@pam"
    insecure: true

  ssh_connection:
    username: "opentofu"
    private_key_path: "~/.ssh/proxmox_opentofu"
    agent: false

  defaults:
    target_node: pve-1
    file_storage: local
    block_storage: local-zfs

  nodes:
    pve-1:
      node_name: "pve-1"
      address: "pve-1.zimmermann.eu.com"
      port: 22
