# Environment file for docker compose
# Generated by OpenTofu (infrastructure/templates/30-cloud-init/omni.docker.env.tftpl)

# System configuration (from YAML vars)
TZ=${tz}
LANG=${lang}
LC_ALL=${lc_all}

# Omni configuration (from ci_secrets)
OMNI_ACCOUNT_UUID=${omni_account_uuid}
OMNI_DOMAIN_NAME=${omni_domain_name}
OMNI_EMAIL_ADDRESS=${omni_email_address}

# Auth0Authentication (from ci_secrets)
AUTH0_CLIENT_ID=${auth0_client_id}
AUTH0_ISSUER=${auth0_issuer}
