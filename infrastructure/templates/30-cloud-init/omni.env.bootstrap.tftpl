# Environment file for Omni bootstrap
# Generated by OpenTofu (infrastructure/templates/30-cloud-init/omni.bootstrap.env.tftpl)

# Github token for git clone (from ci_secrets)
GITHUB_TOKEN=${github_token}

# Repository URL to clone from (from YAML vars)
REPO_URL=${repo_url}
