imagePullSecrets:
  # Secret used by Kubernetes to authenticate when pulling the operator image.
  - name: dhi-registry-secret

resources:
  # CloudNativePG operator resources: requests influence scheduling/QoS; limits enforce a hard cap.
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
