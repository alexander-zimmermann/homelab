apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: fritz-exporter
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: fritz-exporter
  endpoints:
    - port: metrics
      interval: 60s
      scrapeTimeout: 20s
