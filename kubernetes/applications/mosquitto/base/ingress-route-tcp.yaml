apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP

metadata:
  name: mosquitto
  namespace: mosquitto
spec:
  entryPoints:
    - mqtt
  routes:
    - match: HostSNI(`PLACEHOLDER`)
      services:
        - name: mosquitto
          port: 8883
  tls:
    secretName: mqtt-tls-cert
