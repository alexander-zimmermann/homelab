apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: telegraf
resources:
  - ./namespace.yaml
  - ./servicemonitor.yaml
  - ./sealed-secret.yaml

helmCharts:
  - name: telegraf
    repo: https://helm.influxdata.com
    releaseName: telegraf
    version: 1.8.69
    valuesFile: values.yaml
    namespace: telegraf

configMapGenerator:
  - name: telegraf-inputs
    options:
      disableNameSuffixHash: true
    files:
      - inputs/knx.conf
      - inputs/mqtt.ems.conf
      - inputs/mqtt.warp.conf
      - inputs/mqtt.solaredge.conf

labels:
  - includeSelectors: false
    pairs:
      homelab.app: telegraf

commonAnnotations:
  argocd.argoproj.io/sync-wave: "10"
