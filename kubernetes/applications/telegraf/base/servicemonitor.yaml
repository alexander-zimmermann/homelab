apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: telegraf
  labels:
    release: prometheus

spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: telegraf
  endpoints:
    - port: prometheus-client
      interval: 60s
      scrapeTimeout: 20s
