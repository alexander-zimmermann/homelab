---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cilium
resources:
  - ./namespace.yaml

helmCharts:
  - name: cilium
    repo: https://helm.cilium.io
    releaseName: cilium
    version: 1.18.5
    valuesFile: values.yaml
    includeCRDs: true
    namespace: cilium
