http:
  # Add router
  routers:
    ai-pro-4:
      rule: Host(`ai-pro-4.zimmermann.sh`)
      entryPoints: websecure
      tls:
        certresolver: cloudflare
      middlewares: chain-add-1FA-BSC@file
      service: ai-pro-4

  # Add service
  services:
    ai-pro-4:
      loadBalancer:
        servers:
          - url: https://ai-pro-4.zimmermann.eu.com
        serversTransport: ai-pro-4

  # Configure transport between Traefik and http server
  serversTransports:
    ai-pro-4:
      insecureSkipVerify: true
