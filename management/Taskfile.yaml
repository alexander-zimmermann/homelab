---
version: "3"

vars:
  # Colors
  BLUE: '\033[0;34m'
  GREEN: '\033[0;32m'
  YELLOW: '\033[0;33m'
  RED: '\033[0;31m'
  RESET: '\033[0m'

tasks:
  deploy:
    desc: "Deploy Omni Stack via Docker Compose (Local Execution)"
    cmds:
      - cmd: printf "{{.BLUE}}🚀 Deploying Omni Stack locally...{{.RESET}}\n"
        silent: true
      - cd /opt/omni && docker compose up -d
      - cmd: printf "{{.GREEN}}✅ Omni deployed on management VM!{{.RESET}}\n"
        silent: true
